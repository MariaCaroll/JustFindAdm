<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.justfindadm">
    <!-- permissao internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- permissao galeria/camera -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/name_visivel"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustFindAdm">
        <activity android:name=".atividades.CadatrarProfissionalActivity" />
        <activity
            android:name=".atividades.ListarProfissionalActivity"
            android:label="@string/title_activity_cadastro_profissional"
            android:theme="@style/Theme.JustFindAdm.NoActionBar" />
        <activity android:name=".atividades.CadastroActivity" />
        <activity android:name=".atividades.ProfissionalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>